
```bash
openssl genpkey -algorithm Ed25519 -out private.key
```

```bash
openssl pkey -in private.key -pubout -outform DER | tail -c 32 > public.key
```

```bash
# Sign binary and output base64 signature
openssl pkeyutl -sign -inkey private.key -rawin -in <binary_file> | base64
```